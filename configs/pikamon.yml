pikamon:
  bot:
    maximum-spawn-duration: 300s
    spawn-chance: 0.08
    max-pokemon-id: 807
  cache:
    channel:
      number-counters: 100
      max-cost: 100
      buffer-size: 64
  logging:
    level: debug
  store:
    type: sqlite
    sqlite:
      location: ./data/sqlite/pikamon.db
